# Linux运维学习日志 

> 学习时间：2026-08-22


## 今日核心命令


### 一、tar - 打包与压缩

| 用法 | 命令 | 说明 |
| :--- | :--- | :--- |
| 打包并压缩(gzip) | `tar -zcvf 压缩包名.tar.gz 要打包的目录/` | `z`=gzip算法，`c`=创建，`v`=显示过程，`f`=指定文件名 |
| 打包并压缩(bzip2) | `tar -jcvf 压缩包名.tar.bz2 要打包的目录/` | `j`=bzip2算法，压缩率更高但速度更慢 |
| 解压 .tar.gz | `tar -zxvf 压缩包名.tar.gz` | `x`=解压 |
| 解压到指定目录 | `tar -zxvf 压缩包名.tar.gz -C /目标路径` | `-C` 指定解压位置，必须先存在 |
| 仅打包不压缩 | `tar -cvf 包名.tar 目录/` | 不带 `z` 或 `j` |
| 查看包内容 | `tar -ztvf 压缩包名.tar.gz` | `t`=列出，不解压 |

> **核心原则**：`c`(创建) 和 `x`(解压) 互斥，一个命令只能出现一个；`f` 必须放在选项最后，因为它后面紧跟文件名。


### 二、查看已安装软件（yum/rpm）

| 用法 | 命令 | 说明 |
| :--- | :--- | :--- |
| 列出所有已装包 | `yum list installed` | 带版本号和架构信息，内容较多 |
| 按关键字筛选 | `yum list installed \| grep nginx` | 管道过滤 |
| 仅导出包名 | `rpm -qa --queryformat '%{NAME}\n'` | 干净，每行一个包名 |
| 导出完整包信息 | `rpm -qa --queryformat '%{NAME}-%{VERSION}-%{RELEASE}.%{ARCH}\n'` | 含版本和架构，用于迁移恢复 |
| 查看 yum 操作历史 | `yum history` | 按时间列出所有安装/卸载操作 |
| 查看某次操作详情 | `yum history info <交易ID>` | 看具体装了哪些包 |


## 今日完成事项

- [x] 复习 tar 命令（压缩/解压核心用法 + -C 指定目录）
- [x] 掌握 rpm -qa 导出软件清单（为后续迁移打基础）
- [x] 确定服务器策略：本地虚拟机先练，项目跑通后再上云
- [x] 确定项目方向：部署 AI 对话网站（MinAI 优先）


  今天学的不多 主要是在考虑以后的项目要不要上云 感觉上云会好一点
