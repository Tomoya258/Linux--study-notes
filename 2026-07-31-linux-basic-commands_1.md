# Linux 基础命令学习笔记

> 学习日期：2026-07-31
> 主题：文本处理与命令协作基础

---

## 1. echo 命令（输出信息）

**作用**：在终端打印文本或变量值，是系统的"传声筒"，常用于人机交互和信息展示。

```bash
echo "Hello Linux"
echo $PATH          # 打印环境变量
echo -e "line1\nline2"   # -e 参数解析转义字符（如换行符）
```

**常见用途**：
- 脚本中打印提示信息、调试信息
- 配合重定向写入文件内容
- 打印变量值，排查脚本问题

---

## 2. 反引号 `（命令替换）

**作用**：将反引号内的内容作为命令执行，并用执行结果替换该位置，实现命令间的数据传递和动态内容生成。

```bash
today=`date +%Y-%m-%d`
echo "今天是 $today"
```

**扩展**：现代写法更推荐用 `$(command)` 代替反引号，可读性更好、支持嵌套：

```bash
today=$(date +%Y-%m-%d)
echo "今天是 $today"

# 嵌套示例（反引号不支持嵌套，$() 支持）
echo "当前目录文件数：$(ls $(pwd) | wc -l)"
```

---

## 3. 重定向符（数据写入）

**作用**：把命令的输出结果写入文件，是实现信息持久化存储的"搬运工"。

| 符号 | 作用 |
|------|------|
| `>`  | 覆盖写入（原内容会被清空） |
| `>>` | 追加写入（在文件末尾追加，不影响原内容） |
| `<`  | 输入重定向，把文件内容作为命令输入 |
| `2>` | 错误输出重定向，只重定向报错信息 |

```bash
echo "hello" > file.txt      # 覆盖写入
echo "world" >> file.txt     # 追加写入
command 2> error.log         # 把报错信息单独存到日志文件，不影响正常输出
```

**注意**：`>` 要小心使用，操作前最好先确认文件内容，避免误删重要数据。

---

## 4. tail 命令（查看与跟踪）

**作用**：查看文件尾部内容，并可实时跟踪文件更新，是监控日志文件的"观察者"。

```bash
tail file.txt          # 默认查看最后10行
tail -n 20 file.txt     # 查看最后20行
tail -f /var/log/messages   # 实时跟踪文件更新（常用于监控日志）
tail -F /var/log/nginx/access.log   # -F 在日志被切割/重建后仍能继续跟踪（比 -f 更稳）
```

**运维场景**：`tail -f` 是排查线上问题时最常用的命令之一，用来实时观察服务日志、报错信息。

---

## 5. grep 命令（文本搜索）

**作用**：在文件中搜索匹配关键字的行，是信息过滤的"瑞士军刀"。

```bash
grep "root" /etc/passwd        # 搜索包含 root 的行
grep -n "root" /etc/passwd     # -n 显示行号
grep -i "ROOT" /etc/passwd     # -i 忽略大小写
grep -v "root" /etc/passwd     # -v 反向匹配（显示不包含 root 的行）
grep -r "error" /var/log/      # -r 递归搜索整个目录
grep -c "error" file.log       # -c 统计匹配的行数
```

**常用选项速查**：

| 选项 | 作用 |
|------|------|
| `-n` | 显示行号 |
| `-i` | 忽略大小写 |
| `-v` | 反向匹配（排除） |
| `-r` | 递归搜索目录 |
| `-c` | 统计匹配行数 |
| `-E` | 支持扩展正则表达式 |

---

## 6. wc 命令（内容统计）

**作用**：统计文件的行数、单词数、字节数，快速获取文本内容概览。

```bash
wc file.txt        # 同时显示 行数 单词数 字节数
wc -l file.txt      # 只统计行数
wc -w file.txt      # 只统计单词数
wc -c file.txt      # 只统计字节数
```

**运维场景**：常用来快速统计日志行数、配置文件条目数量等。

---

## 7. 管道符 |（命令协作）

**作用**：将左侧命令的输出，作为右侧命令的输入，实现命令间的协作，一步步把数据"加工"成想要的结果。

> 设计哲学：**Do One Thing and Do It Well**（每个命令只做好一件事，通过管道组合完成复杂任务）

```bash
cat file.txt | grep "keyword" | wc -l    # 统计文件中含关键字的行数
ls -l | grep ".log" | wc -l              # 统计当前目录下 .log 文件的数量
ps -ef | grep nginx                      # 查找 nginx 相关进程
history | grep "yum"                     # 从历史命令中查找用过的 yum 命令
```

**理解管道的关键**：管道左边命令的**标准输出**，直接变成右边命令的**标准输入**，命令之间层层过滤、层层加工，这正是 Linux 命令行的核心设计思想。

---

## 8. 组合应用示例

```bash
# 统计文件中含关键字的行数
cat file.txt | grep "keyword" | wc -l

# 统计当前目录下 .log 文件的数量
ls -l | grep ".log" | wc -l

# 实时监控日志中的报错信息
tail -f app.log | grep -i "error"

# 查找并统计某进程数量
ps -ef | grep -v grep | grep nginx | wc -l

# 把某次搜索结果保存到文件
grep "error" app.log > error_only.log

# 追加当天日期到日志文件末尾，方便按天区分
echo "===== $(date +%Y-%m-%d) =====" >> app.log
```

---

## 总结

这几个命令是 Linux 文本处理的核心基础，共同点是**都遵循"单一职责"的设计哲学**——每个命令只做一件事，通过管道符 `|` 和重定向符 `>`/`>>` 灵活组合，就能完成复杂的文本处理和日志分析任务。这也是运维日常排查问题时最常用的命令组合方式。

**下一步学习建议**：
- 熟悉 `sed`、`awk` 这两个更强大的文本处理工具
- 结合 `find` 命令做文件查找与批量处理
- 练习用这些命令写简单的 Shell 脚本，自动化处理日志分析任务
